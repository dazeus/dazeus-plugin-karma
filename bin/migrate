#!/usr/bin/env node
var path = require('path');
var source_path = path.join(path.dirname(__dirname), 'src');

require('traceur').require.makeDefault(function (file) {
  return -1 !== file.indexOf(source_path);
});
require('../src/migrate/main');
